# NIXPACKS config for Luxe Jewelry API
# Build context chỉ trong folder api/ (set Root Directory trên Railway).

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node scripts/migrate.js && node server.js"