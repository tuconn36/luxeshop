{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["api/**"]
  },
  "deploy": {
    "startCommand": "cd api && npm install && node index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}