{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "build": "npm install --prefix api",
    "start": "npm start --prefix api",
    "start:web": "npm run preview --prefix web",
    "lint": "concurrently --raw \"npm run lint --prefix web\"",
    "install:all": "npm install --prefix api && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
